{
  "name": "canvas-app-recorder",
  "version": "0.1.0",
  "buildParams": {
    "libName": "canvas-app-recorder",
    "namespace": "CanvasAppRecorder"
  },
  "description": "Record application using canvas and webaudio",
  "main": "./dist/canvas-app-recorder.js",
  "module": "./dist/canvas-app-recorder.esm.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rimraf dist && rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "canvas",
    "webaudio",
    "MediaStream"
  ],
  "author": "pentamania",
  "license": "MIT",
  "dependencies": {
    "@types/dom-mediacapture-record": "^1.0.1"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "rollup": "^1.9.0",
    "rollup-plugin-license": "^0.8.1",
    "rollup-plugin-typescript2": "^0.21.0",
    "rollup-plugin-uglify": "^6.0.2",
    "tslib": "^1.9.3",
    "typescript": "^3.4.2"
  }
}
